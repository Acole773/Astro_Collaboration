/*
 *	pull in information from Einstein toolkit (mass fraction {X_i}, total mass (M_total, temperature, and density)
 *
 *	create a while loop where t<t_final
 *	this while loop will update the popluations (y_old), the partition function (PF), compute reaction 
 *	fluxes, and get the max timestep
 *
 *	for integration step: 
 *	-sum mass fractions
 *	-network mass difference
 *
 */

